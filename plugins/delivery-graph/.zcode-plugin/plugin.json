{
  "name": "delivery-graph",
  "version": "0.39.21",
  "description": "确认 Git 基线，以显式 rootId 管理多 Delivery，在当前 workspace 串行调度 GROUP/TASK，并支持冻结开发包、验收与恢复",
  "author": {
    "name": "delivery-graph contributors",
    "url": "https://github.com/feng123321li/layered-delivery"
  },
  "homepage": "https://github.com/feng123321li/layered-delivery",
  "repository": "https://github.com/feng123321li/layered-delivery",
  "license": "MIT",
  "keywords": [
    "ai-development",
    "graph-scheduling",
    "recursive-graph",
    "manual-handoff",
    "review-governance",
    "automatic-agent-dispatch",
    "serial-workspace",
    "group-task",
    "task-loop"
  ],
  "skills": "./skills/",
  "mcpServers": {
    "delivery-graph": {
      "command": "python",
      "args": [
        "-X",
        "utf8",
        "${ZCODE_PLUGIN_ROOT}/skills/delivery-graph/scripts/hdg_mcp.py"
      ],
      "cwd": "${ZCODE_PLUGIN_ROOT}",
      "env": {
        "HDG_HOST_ADAPTER": "zcode",
        "HDG_PROJECT_ROOT": "${ZCODE_PROJECT_DIR}"
      },
      "supports_parallel_tool_calls": false,
      "default_tools_approval_mode": "approve",
      "tools": {
        "rebuild_graph_run": {
          "approval_mode": "prompt"
        },
        "cancel_graph_run": {
          "approval_mode": "prompt"
        },
        "archive_delivery": {
          "approval_mode": "prompt"
        },
        "unfreeze_task_requirement": {
          "approval_mode": "prompt"
        },
        "refreeze_task_requirement": {
          "approval_mode": "prompt"
        },
        "handoff_ready_automatic_task": {
          "approval_mode": "prompt"
        }
      }
    }
  },
  "interface": {
    "displayName": "分层交付 Graph 控制面",
    "shortDescription": "多 Delivery 串行 workspace、Graph 调度与分层审查",
    "longDescription": "每个 Delivery 使用稳定 rootId 和不可变 Revision 管理需求与验收；同一物理 checkout 可绑定多个 Delivery，但只允许队首 Run 执行。前序 receiver 停止、产生可验证业务 commit 且工作树干净后，后续 Delivery 才能切换自己的冻结分支并恢复。自动 TASK 与 Review 继续使用 reservation、operation 租约和完整分层治理，Controller 还会生成可直接审核的 workspace changes patch。",
    "developerName": "delivery-graph contributors",
    "category": "Productivity",
    "capabilities": [
      "Read",
      "Write"
    ],
    "websiteURL": "https://github.com/feng123321li/layered-delivery",
    "defaultPrompt": [
      "使用 delivery-graph 确认 Git 基线，以显式 rootId 管理多个 Delivery，并在当前 workspace 中串行执行、提交、审查与恢复。"
    ],
    "brandColor": "#2563EB",
    "composerIcon": "./assets/delivery-graph-icon.png",
    "logo": "./assets/delivery-graph-icon.png"
  }
}
