interface:
  display_name: "递归交付调度"
  short_description: "预览递归 Graph，自动调度或生成开发内容交接文件"
  default_prompt: "使用 $layered-delivery 只读预览递归交付 Graph；自动执行时再创建开发 worktree、准备冻结并按宿主容量与 Agent 分析选择原生模型，手动交接时只生成一个不绑定 Agent/模型的自包含文件。"

policy:
  allow_implicit_invocation: true
