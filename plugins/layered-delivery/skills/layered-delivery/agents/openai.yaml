interface:
  display_name: "递归交付调度"
  short_description: "调度递归 Graph，并展示本机 Agent + Model 建议"
  default_prompt: "使用 $layered-delivery 恢复或创建递归交付 Graph，并展示每个 TASK/Review 的本机 Agent + Model 建议。"

policy:
  allow_implicit_invocation: true
