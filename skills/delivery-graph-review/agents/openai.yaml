interface:
  display_name: "Delivery Graph Review Receiver"
  short_description: "独立验证一个 Review Loop 的分层验收结论"
  default_prompt: "使用 $delivery-graph-review 独立验证当前 assignment 指定的 Review Loop。"

policy:
  allow_implicit_invocation: false
