interface:
  display_name: "分层交付 Graph 控制面"
  short_description: "规划需求、确认 Git 基线并冻结 Delivery Graph"
  default_prompt: "使用 $delivery-graph 规划 schema v3 Delivery Graph、确认 Git 基线并选择执行模式。"

policy:
  allow_implicit_invocation: true
