interface:
  display_name: "门禁式 AI 开发循环"
  short_description: "统一目录、可视进度、自动并行开发、冻结门禁与独立验收工作流"
  default_prompt: "使用 $gated-ai-dev-loop 路由并冻结这个开发任务，符合并行资格时让我选择 single 或 parallel；选择 active + parallel 后自动派遣子 Agent，再执行聚合门禁和独立验收。"
