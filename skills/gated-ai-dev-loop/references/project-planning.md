# 大型完整项目规划

## 目录

- [适用条件](#适用条件)
- [规划产物](#规划产物)
- [分层拆解](#分层拆解)
- [project-plan.md 模板](#project-planmd-模板)
- [拆解质量门禁](#拆解质量门禁)
- [执行与回写](#执行与回写)
- [变更控制](#变更控制)

## 适用条件

`Project` 是工作规模，不是新的 CLI 模式。命中任一情况时使用 `Full · Project · 主要变更类型`：

- 用户要求从零到可验收结果的完整系统、平台、应用或大模块开发；
- 需求包含多个可以独立验收的里程碑、业务域或工作流；
- 多个服务、客户端、数据层、基础设施或发布阶段必须按依赖顺序集成；
- 单一实现轮次无法安全覆盖全部需求和验收；
- 用户明确要求总体方案、路线图、阶段交付或完整项目计划。

不要因为任务使用 Full 就自动判为大型项目。单个高风险 API、迁移或权限改动仍可使用普通 Full。

## 规划产物

在用户确认基线前建立。CLI 可能替换任务目录，因此冻结前先写入 `.ai-dev-loop/.host-staging/<task-id>/` 的同构相对位置，冻结成功后再物化到任务目录。staging 版链接必须可解析或明确标为冻结后目标；物化时根据结构化计划重新渲染最终相对链接并核对来源/输出摘要：

1. 最终任务根级 `development-overview.md`：面向人的稳定项目开发总纲；
2. `rounds/planning/project-plan.md`：详细里程碑、工作流、原子任务、依赖与验收映射；
3. 最终任务根级 `progress.md`：实时任务与 SOP 状态；
4. Full `baseline.md`、`tasks.json` 和 `acceptance.json`：唯一冻结开发授权。

协调工作区根级另由 [task-registry.md](task-registry.md) 维护跨任务 `task-registry.json` 与 `workspace-overview.md`，不要把它们和单个 Project 的开发总纲混为一份。

总纲和项目计划不能取代基线。发生冲突时以用户确认并冻结的基线为准；规划必须修正到与基线一致后才能继续。

## 分层拆解

使用四层标识，禁止混用：

- `M-NNN` Milestone：用户可观察的阶段成果；
- `W-NNN` Workstream：围绕一个业务域、组件或集成目标的工作流；
- `T-NNN` Executable Task：一个开发上下文能够实现并独立验证的最小交付任务；
- `S-NNN` SOP Step：恢复、分析、确认、冻结、授权、派遣、门禁、验收和反馈处理等流程步骤。

每个 `T-NNN` 至少写明：

- 所属 M/W；
- 关联 R/A；
- 目标工作区和仓库相对允许路径；
- `dependsOn`；
- 输入、输出和对外契约；
- 测试 argv 或由哪个集成门禁覆盖；
- 可观察的完成定义；
- 主要风险和阻断条件。

一个任务如果同时跨越多个不可独立回滚的目标、包含未解决设计决策，或无法给出单独完成证据，就继续拆分。不要按“前端一个任务、后端一个任务”机械拆分；优先按可验收的纵向成果和真实依赖拆分。

## project-plan.md 模板

```markdown
# <task-id> 项目开发计划

> task record revision: <recordRevision>

## 规划判定
- 门禁等级：Full
- 工作规模：Project（完整项目）
- 规模代表说明：多个能力和里程碑组成一个完整项目
- 当前项目说明：<本项目包含的能力与里程碑摘要>
- 变更类型：<Feature / Migration / 其他主要类型>
- 判定依据：<完整项目、多里程碑或多工作流事实>
- 权威基线：[../../baseline.md](../../baseline.md)
- 开发总纲：[../../development-overview.md](../../development-overview.md)
- 实时进度：[../../progress.md](../../progress.md)

## 最终成果与边界
- 最终成果：<用户可观察结果>
- 范围：<业务、系统和仓库边界>
- 非目标：<明确排除项>
- 架构与兼容约束：<关键约束及决策链接>

## 里程碑
| 里程碑 | 可交付结果 | 包含工作流 | 进入条件 | 完成门禁 | 依赖 |
| --- | --- | --- | --- | --- | --- |
| M-001 | <可演示或可验收结果> | W-001 | <事实> | A-001、测试 argv | 无 |

## 工作流
| 工作流 | 目标 | 工作区 / 服务 | 任务 | 集成点 | 依赖 |
| --- | --- | --- | --- | --- | --- |
| W-001 | <单一工作流目标> | service-a | T-001、T-002 | <契约或数据流> | 无 |

## 可执行任务
| 任务 | M / W | R / A | 工作区与允许路径 | dependsOn | 输入 → 输出 | 测试 / 完成定义 | 风险 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| T-001 | M-001 / W-001 | R-001 / A-001 | service-a: src/** | 无 | <输入> → <输出> | <argv；可观察结果> | <风险> |

## 依赖与关键路径
- 波次 1：<任务及原因>
- 波次 2：<依赖前置输出的任务>
- 跨工作区契约：<提供方、消费方和验证方式>
- 关键路径与可并行项：<事实，不使用主观估计>

## 集成与验收节奏
- 每个任务：<局部测试和证据>
- 每个里程碑：<聚合门禁和演示结果>
- 最终：<全量机械门禁、语义验收和人工确认>

## 风险、决策与回退
| ID | 风险 / 决策 | 影响 | 缓解或回退 | 责任方 | 证据 |
| --- | --- | --- | --- | --- | --- |
| P-001 | <事实> | <范围> | <措施> | <角色> | <链接> |

## 变更控制
- 需求内修复：沿用当前基线并开修复轮次。
- 目标或验收变化：按人工反馈规则创建关联修订并重新确认。
- 计划重排：更新本文件和 progress 时间线；不得暗改冻结 R/A/T。
```

## 拆解质量门禁

请求用户确认基线前逐项检查：

- 每个 R 都被至少一个 A 和 T 覆盖；
- 每个 A 都有可运行门禁或明确人工观察点；
- 每个 T 都属于一个 M/W，并有单一可观察结果；
- 每个写入任务都绑定工作区和允许路径；
- 所有提供方/消费方、数据、契约和集成依赖均有方向；
- 依赖图无环，关键路径和可并行项有事实依据；
- 每个里程碑都有进入条件、完成门禁和失败后的处理；
- 风险、未决选项和外部前置条件已经解决或明确阻断；
- `development-overview.md`、`project-plan.md`、baseline、tasks 和 acceptance 互相一致；
- `progress.md` 已初始化全部 M/W/T 和 SOP 行。

任一项不满足时保持 `WAITING_FOR_REQUIREMENT_CONFIRMATION`，列出缺口，不进入开发方式选择。

## 执行与回写

宿主是注册表与进度投影唯一写入者。每次状态变化按以下顺序执行：

1. 先收集真实外部结果或阻断事实，不在锁外创建或覆盖宿主规范 evidence；
2. 在根级单写锁内复核 revision，以 create-new 写规范 evidence 与目标 record revision 的不可变 lifecycle event，再更新 `task-registry.json` 对应任务的 phase、周期、完成计数、下一责任方和阻断项；
3. 把 task projections 标为 `PENDING` 并重建根级 `workspace-overview.md`；
4. 更新 `project-plan.md`、`progress.md` 对应 M/W/T 或 S 行及 task revision marker，并向时间线追加带时间和证据链接的事件；
5. 核对摘要后提交 projection ack、标为 `CURRENT`，再重建最终工作区总纲；
6. 再派遣下一任务或向用户报告。

不得把开发 Agent 的“已完成”直接写成 `VERIFIED`。开发结果存在时标记 `IMPLEMENTED`；机械和语义证据通过后标记 `VERIFIED`；用户最终接受后才完成整个项目。

规范注册表写回失败时不得口头宣称状态已经推进，停止并报告 `TASK_REGISTRY_WRITE_FAILED`；注册表成功但总纲或进度投影失败时报告 `TASK_REGISTRY_PROJECTION_FAILED`，下次进入先从 registry 重建投影。

## 变更控制

- 计划语义细化但不改变冻结授权：写 `PLAN_REFINED` lifecycle event 并增加 task record revision，按完整 registry/projection 事务重渲染 `project-plan.md`、`workspace-overview.md` 和 `progress.md`，不删除旧证据。仅由模板版本触发、结构化事实完全不变的确定性排版重渲染不写 task event、可保持 task record revision，但仍需 workspace projection ack 与摘要；不得手工改一份 Markdown 后跳过注册表；
- 调整任务顺序或责任方：重新检查依赖、路径互斥和工作区覆盖；
- 新增行为、验收、工作区或里程碑：不是普通计划更新，按 `REVISE_CURRENT` 重新确认；
- 删除或延期冻结范围：取得用户确认并记录 disposition；
- 创建 follow-up：保留原项目进度和证据，显式记录关系。
