interface:
  display_name: "递归交付调度"
  short_description: "调度递归 GROUP/TASK、Review Loop 与交付 Graph"
  default_prompt: "使用 $layered-delivery 检查当前状态，并恢复或创建递归 GROUP/TASK 交付 Graph。"

policy:
  allow_implicit_invocation: true
