interface:
  display_name: "分层交付调度"
  short_description: "调度多项目、多模块的 Task Loop 与交付 Graph"
  default_prompt: "使用 $layered-delivery 检查当前调度状态，并恢复或创建最浅合法的交付 Graph。"

policy:
  allow_implicit_invocation: true
