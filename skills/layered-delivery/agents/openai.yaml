interface:
  display_name: "递归交付调度"
  short_description: "调度带 Git 分支绑定的递归 Graph，并展示 Agent + Model 建议"
  default_prompt: "使用 $layered-delivery 在独立 Delivery worktree/feature 分支中恢复或创建递归交付 Graph，并展示每个 TASK/Review 的本机 Agent + Model 建议。"

policy:
  allow_implicit_invocation: true
