"""Layered Delivery.

The package intentionally depends only on the Python standard library so the same
MCP controller can run from the repository or from an installed Plugin payload.
"""

__version__ = "0.16.5"
